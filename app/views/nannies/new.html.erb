

保母

<%= image_tag current_user.fb_pic , class: "img-circle" %>
<%= current_user.name %>



<%= simple_form_for @nanny do |u|%>
	<%= u.simple_fields_for :user do |f| %>
		<%= f.input :line_id %>
		<%= f.input :phone %>
	<% end %>

	<%= u.input :status %>

	<%= u.simple_fields_for :image do |i| %>
		<%= i.input :image, :as => :file %>
	<% end %>

	<%= u.button :submit %>
<% end %>